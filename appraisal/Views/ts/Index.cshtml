@model PagedList.IPagedList<appraisal.Models.ts>
@using PagedList.Mvc;
<link href="~/Content/PagedList.css" rel="stylesheet" type="text/css" />
@{SelectList itemsPerPageList = appraisal.Models.DefaultValues.ItemsPerPageList;}
@{
    ViewBag.Title = "考核紀錄";
}

<h2>考核紀錄</h2>

<p>
    @Html.ActionLink("新增", "Create")
    @Html.PagedListPager(Model, page => Url.Action("Index", new { page, itemsPerPage = ViewBag.CurrentItemsPerPage, sortOrder = ViewBag.OldSortParm, searchString = "", CurrentFilter = ViewBag.CurrentFilter })
                    , new PagedListRenderOptions { LinkToFirstPageFormat = "<<首頁", LinkToPreviousPageFormat = "<上一頁", LinkToNextPageFormat = "下一頁>", LinkToLastPageFormat = "末頁>>", MaximumPageNumbersToDisplay = 5 })
</p>
<table class="table">
    <tr>
        <th>
            @Html.ActionLink("卡號", "Index", new { page = 1, itemsPerPage = ViewBag.CurrentItemsPerPage, sortOrder = ViewBag.NameSortParm, searchString = "", CurrentFilter = ViewBag.CurrentFilter })
        </th>
        <th>
            員工姓名
        </th>
        <th>
            @Html.ActionLink("部門名稱", "Index", new { page = 1, itemsPerPage = ViewBag.CurrentItemsPerPage, sortOrder = ViewBag.DateSortParm, searchString = "", CurrentFilter = ViewBag.CurrentFilter })
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().exm1.subject)
        </th>
        <th>
            主管姓名
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().vl)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.emp1.eid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.emp1.cname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.emp1.dep.title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.exm1.subject)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.emp2.cname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.vl)
        </td>
        <td>
            @Html.ActionLink("編輯", "Edit", new { id=item.id }) |
            @Html.ActionLink("明細", "Details", new { id=item.id }) |
            @Html.ActionLink("刪除", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
@if (Model != null && Model.PageCount > 0)
{
    <div>
        Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount
    </div>
}
@using (Html.BeginForm())
{
    if (Model != null)
    {
        <fieldset>
            設定每頁筆數 :
            @Html.DropDownList("ItemsPerPage", itemsPerPageList, new { @id = "ItemsPerPageList" })
            尋找關鍵字: @Html.TextBox("searchString")
            前往頁次: @Html.TextBox("page")
            <input type="submit" value="送出" />
        </fieldset>
    }
}
